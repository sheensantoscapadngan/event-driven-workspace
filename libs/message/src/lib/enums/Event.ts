export enum LogisticEvent {
  DELIVERY_INITIATED = 'logistic.delivery_initiated',
}

export enum PaymentEvent {
  PAYMENT_PROCESSED = 'payment.payment_processed',
}

export enum OrderEvent {
  ORDER_CREATED = 'order.order_created',
}
